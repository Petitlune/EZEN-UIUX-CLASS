먼저 product.js 생성하기
실제 id는 id:  Date.now( ) 이런 식으로 생성

reduce함수 체크박스 클릭
> 상품의 전체 총 금액
> 배열!! / 배열 값추가  ==> push


15 복습

1) 함수
> 매개변수 기본값 설정
> 나머지 매개변수 사용하는 방법

2) 배열
> 서로 다른 배열을 하나로 합치는 방법 [...arr, ...arr]
> 배열을 복제하는 방법 [...arr] =>원.사본 배열의 연관관계 X

3) 객체
> value를 찾아오는 방법 : 온점표기법 / 대괄호표기법(무조건 문자열)
> 객체 key생성시 외부 함수를 활용

4) 심볼
> 심볼 값을 찾아올 때 반드시 대괄호 표시로 찾아와야한다.
> 심볼 전역요소 생성 / 찾아올 수 있음
> 심볼 값을 복제해올 수 있는 방법 Symbol.for( )
> 심볼 값에 매칭되어지는 값을 찾아오는 방법 Symbol.keyFor( )

5) react => 구조 분해 할당 
> 배열 및 객체 가능
> 구조분해할당하고자 하는 자료형의 타입을 값을 받는 요소에서도 동일하게 사용해야함!
> 객체의 경우, 반드시 변수명 === 객체의 key 이름
> 배열의 경우 전개연산자를 활용해서 구조분해할당이 가능하다 