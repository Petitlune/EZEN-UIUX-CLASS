함수 ===> 자바스크립트의 꽃
- 어떤 여러 '기능' 및 '명령'들을 하나로 묶어놓은 집합체 그룹
- html에서 구현가능한 동영상을 찾아와라
- 해당 동영상을 찾아와서 내가 원하는 사이즈로 화면에 구현시켜라

prompt( )
alert( )
document.write( )  
-위 세개의 함수는 내장함수라서 따로 구현을 하지 않아도 바로 호출할 수 있음

함수의 생김새 => 단어( ) 

- 함수가 작동하려면 무엇이 필요할까? 
1. 함수를 구현 = 제작 
- 함수를 구현하려면 어떻게 해야할까? : 총 3가지 방법
ㄴ> 1) class 함수를 구현하는 방법
      2) 익명 함수를 구현하는 방법 (함수의 이름을 정의하지 않은 상태로 구현)
      3) 화살표 함수를 구현하는 방법 
          
2. 함수를 호출 = 실행

*인수 = 인자값


//js코드를 작성하다보면 큰 이슈들은 콘솔창을 통해서 알려줌.
그러나 아주 사소한 문법상의 문제는 알려주지 않음(오타 등등) -> 코드상의 문제 발생 => 버그 발생 : 이러한 버그를 잡기 위해 TS(타임스크립트)가 나옴 

효율적으로 버그 잡는 방법 : 개발자 도구에 Sources 에서 script. js 누르고 함수 클릭 후 새로고침 => 코드를 읽음

=========***********중요**********===========

1) var보다는 let, const 키워드로 변수를 선언해라. var를 갖다 쓰면 전역요소로 전체를 덮어쓰게 됨. 
2) 가급적이면, 전역 스코프의 변수선언(var)은 피해라

Global scope = 전역 스코프 = 해당 변수를 현 자바스크립트 파일 내 어디에서든 사용이 가능한 변수값
Local scope = 지역 스코프 = 지역 방송 = 해당 변수를 특정 영역안에서만 사용할 수 있도록 하는 변수값
Block scope = 블록 스코프 = 해당 변수가 선언된 블록 안에서만 유효한 변수값

*즉시 실행 함수

<함수>
- 클래스 / 익명 / 함수
- 매개변수 & 인수(인자값)
- return(함수 결과값 반환)
- 스코프(블록 / 지역/ 전역)
- var x / 지역스코프 변수 / const(권장)
- 즉시실행함수
- Hoisting
- 1급 시민
- 전개연산자 구문
- 나머지 매개변수

JS 문법 : 이해 => 그러나 문법을 실제로 실행하는 것은 별도의 문제
==> 직접 코드 쳐보면서 복습해야 함. 

window 운영체제 => 내장함수 
JS => 싱글스레드로 작동하는 객체지향 프로그래밍 언어.
 - 싱글 = 1 , 스레드 = 길, 1차선 통행 ==> 동시성이 불가능 (=비동기 처리가 안됨)

<->멀티스레드 : 복수차선


=======figma======
1. figma / 스타일가이드 / 컴포넌트
2. figma / 와이어프레임(프로토타입 *목업) 작업
3. 상세기술서 (*실제 코드를 입력하기 전에 Box- Model)
4. vs code (*js => 인터렉티브)

